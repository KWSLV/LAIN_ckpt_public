# A4 Epoch 9 继承 Object 权重训练记录

- 时间：2026-08-22（Asia/Shanghai）
- 新实验名：`UO_a4_ep9_objinherit_objectonly_r4`
- 初始 checkpoint：`/root/autodl-tmp/Lain/UO_a4all_seed66/checkpoints/ckpt_35055_09.pt`
- Object 初始化：继承 checkpoint 中已有的 Object adapter 权重；不使用 `--reset_obj_cond_adapter_on_load`。
- 训练范围：冻结 LAIN 主体，只训练 Object adapter。
- Text：关闭。
- Scene Gate：关闭。
- Object rank：4。
- Object LR：`2e-4`。
- 其余训练、评估、诊断和 checkpoint 保存参数与 `UO_a4_ep9_objreset_objectonly_0.1lr` 保持一致。
- checkpoint：每轮保存，并保存历史最佳 Unseen checkpoint。
- W&B 名称：与训练脚本名一致，即 `UO_a4_ep9_objinherit_objectonly_r4`。

## 与当前 0.1lr 实验的实质区别

1. 删除 `--reset_obj_cond_adapter_on_load`，因此 Object 权重从 A4 epoch 9 checkpoint 继承。
2. `--lr-obj-cond-adapter` 从 `2e-5` 改为 `2e-4`。
3. 输出目录、W&B run 和 tmux session 使用独立名称，避免覆盖旧实验记录。
