# 第 7 轮 Object LR ×2 连续续训记录

- 修改时间：2026-08-21 16:25:10（Asia/Shanghai）
- 实验目录：`/root/autodl-tmp/Lain/UO_lain5_object15_dynamic_retry20_seed66`
- 原 W&B Run ID：`j7q84llg`
- 续训起点：逻辑第 6 轮已接受 checkpoint `ckpt_27265_06.pt`
- 续训范围：逻辑第 7～20 轮

## 修改内容

1. Object 当前学习率由 `1e-4` 乘 2，改为 `2e-4`。
2. LAIN Head 当前学习率保持 `2.5e-4`，ViT 当前学习率保持 `1e-4`。
3. 不覆盖第 6 轮原 checkpoint；复制生成只修改 Object optimizer param group LR 的续训 checkpoint。
4. 保留模型参数、AdamW 动量、AMP scaler、iteration、epoch 和已有最优状态。
5. 新增同一实验目录续训入口：从 `staged_retry_state.json` 恢复历史 best、阈值触发状态、rollback 数和 attempt index，从逻辑第 7 轮继续。
6. 使用 `WANDB_RUN_ID=j7q84llg`、`WANDB_RESUME=must` 恢复原 W&B run；终端输出追加到原训练日志，JSON/JSONL 和诊断文件继续写入原目录。
7. 固定 scheduler 仍关闭；后续 rollback 继续在当时实际 LR 基础上乘 `0.5`，首次达到 Unseen 37.5 的一次性触发仍按初始基准 LR 的 `1/10` 执行。

## 代码改动

- `utils/args.py`：增加 `--staged-resume-existing-run`。
- `main.py`：允许受控恢复同一个 staged run。
- `engine.py`：恢复 staged 策略状态与逻辑 epoch，输出恢复后的实际 optimizer LR。
- `scripts/training/UO_lain5_object15_dynamic_retry20.sh`：支持 `RESUME_CHECKPOINT`，复用原输出目录续训。

## 连贯性约束

- 第 1～6 轮数据不重写。
- 第 7 轮只从第 6 轮已接受状态开始，停止前第 7 轮尚未执行参数更新。
- W&B 正常曲线仍只记录最终接受的逻辑 epoch，失败 retry 不写入正常曲线。

## 启动确认

- 恢复时间：2026-08-21 16:30:01 CST
- 后台脚本 PID：`575391`
- GPU 训练进程 PID：`575405`
- 日志已确认：`completed_epoch=6`，从 `logical epoch 7/20` 开始。
- 实际 optimizer LR 已确认：Head=`2.5e-4`、ViT=`1e-4`、Object=`2e-4`。
- W&B 已显示 `Resuming run UO_lain5_object15_dynamic_retry20`，Run ID 仍为 `j7q84llg`。
