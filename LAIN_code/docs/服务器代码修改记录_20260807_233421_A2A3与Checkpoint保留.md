# 服务器代码修改记录：A2/A3 与 checkpoint 保留

- 时间：2026-08-07 23:34:21（UTC+8）
- 服务器项目：`/root/Lain/LAIN-main`
- 修改状态：完成
- 是否启动训练：本次修改未启动 A2/A3

## 修改文件

- `/root/Lain/LAIN-main/engine.py`
- `/root/Lain/LAIN-main/utils/args.py`

## 新增文件

- `/root/Lain/LAIN-main/scripts/training/UO_a2_a3_common.sh`
- `/root/Lain/LAIN-main/scripts/training/UO_a2_object_only_r4_background.sh`
- `/root/Lain/LAIN-main/scripts/training/UO_a3_text_object_r4a16_background.sh`

## 修改前备份

`/root/Lain/LAIN-main/.codex_backups/20260807_233421_a2_a3_checkpoint_retention`

## 修改内容与实验定义

1. 新增 `--keep-best-unseen-checkpoint`。
2. 验证 unseen 指标创新高时保存 `best_unseen.pt`，并写入对应元数据 JSON。
3. 同时保留最新一轮 checkpoint，满足“最高 unseen 一轮 + 最后一轮”的对照需求。
4. A2：Visual Adapter + Object-conditioned Adapter；Text Adapter 和 SceneGate 关闭。
5. A3：Visual Adapter + Text Semantic Adapter + Object-conditioned Adapter；SceneGate 关闭。
6. 每轮评估开启 adapter contribution diagnostics；SceneGate 关闭的实验不把 SceneGate 指标当作模块效果依据。

## 关键参数

- A2 Object rank：4。
- A3 Text rank/alpha：4/16。
- A3 Object rank：4。
- `--adapter-contribution-diagnostics`：开启。
- `--keep-best-unseen-checkpoint`：开启。

## 验证结果

- Python 语法和 Shell 语法检查通过。
- 模拟 3 轮 retention：第 2 轮 best-unseen 得到保留，第 3 轮 latest checkpoint 得到保留。

## 回滚方法

用备份目录恢复 `engine.py` 和 `utils/args.py`；新增脚本可停用，不删除用户训练产物。
