# Gate第1轮继承精修：贡献触发动态学习率

- 修改时间：2026-08-20（Asia/Shanghai）
- 服务器代码目录：`/root/Lain/LAIN-main`
- 训练脚本：`UO_objectbest_gateonly_v5h128_a004.sh`

## 本次训练

- 来源：`/root/autodl-tmp/Lain/UO_objectbest_gateonly_v5h128_a004_seed66/checkpoints/ckpt_03895_01.pt`
- 使用`--init_from`加载checkpoint模型权重，不恢复旧优化器状态。
- 不使用`--reset_scene_gate_on_load`，因此继承checkpoint中的Gate权重。
- 冻结LAIN主体和Object Adapter，关闭Text Adapter，仅训练普通V5 H128 Gate。
- `scene_gate_alpha=0.04`
- Gate初始学习率：`5e-5`
- 若每轮`Gate ON Unseen - Gate OFF Unseen > 0.10`，则下一轮Gate学习率永久降低到`1e-5`。
- 默认训练6轮；禁用周期内StepLR衰减（`lr_drop=100`），避免与贡献调度混杂。
- 新输出目录：`/root/autodl-tmp/Lain/UO_objectbest_gateonly_v5h128_a004_refine_from_ep01_lr5e5_seed66`

## 代码修改

- `utils/args.py`：增加Gate贡献学习率调度参数。
- `main.py`：增加参数组合及数值范围校验。
- `engine.py`：根据每轮Gate Unseen贡献单调降低Gate优化器LR，同时同步StepLR状态，并写入JSON、CSV、日志和W&B。
- 新增后台启动脚本：`UO_objectbest_gateonly_v5h128_a004_refine_background.sh`。

## 启动要求

启动前停止服务器其他训练；验证GPU空闲、来源checkpoint存在且脚本通过语法检查后再启动本任务。

## 实际启动状态

- 启动时间：2026-08-20 02:17:29（服务器时间）
- tmux：`lain_objectbest_gate_refine_lr5e5_s66`
- 日志：`/root/autodl-tmp/Lain/UO_objectbest_gateonly_v5h128_a004_refine_from_ep01_lr5e5_seed66/logs/train_20260820_021729.log`
- 启动前检查未发现其他训练或GPU计算进程，因此没有需要强制终止的任务。
- checkpoint检查确认包含8个普通V5 H128 Gate张量。
- 启动日志确认`reset_scene_gate_on_load=False`，没有跳过Gate权重。
- 可训练参数仅8个Gate张量，共133,377个参数；LAIN主体和Object均已冻结。
